# AI Provider Configuration
# Set one of these to enable AI features. Without a key, mock mode is used.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Server
PORT=3001

# Database (Neon Postgres via Vercel integration)
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Neon Auth (server-side — set BASE_URL or JWKS_URL)
NEON_AUTH_BASE_URL=
NEON_AUTH_JWKS_URL=

# Neon Auth (client-side, exposed to browser)
VITE_NEON_AUTH_URL=
